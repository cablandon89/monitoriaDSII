#include <iostream>
#include <string.h>
// #include "menu.cpp" // llama otro archivo que se alla creado
// /*realizar un sofware en c++ que sea tipo menu restaurante dicho sofware debe contener el menu en una matriz tipo string
// y el acceso al mismo debe ser por medio de funciones, el menu debe ser mostrado en pantalla y seleccionado por el usuario*/
using namespace std;

void mostrar(void);
string menu(string hamburguesa);
string menu(string salchipapa);
string menu(string gaseosa);
int pedidos(int seleccionPedido);

int main()
{
	mostrar();
	int seleccionPedido();
	return 0;
}
